# Core dependencies
numpy>=1.24.0
requests>=2.28.0
beautifulsoup4>=4.12.0

# Optional: better text embeddings (falls back to TF-IDF/BoW without these)
# sentence-transformers>=2.2.0

# Optional: image embeddings via CLIP (falls back to pixel features without these)
# transformers>=4.30.0
# torch>=2.0.0
# Pillow>=9.0.0

# Optional: TF-IDF fallback
# scikit-learn>=1.2.0

# Testing
pytest>=7.0.0
